{
  "name": "qr-mogging",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PostHog/qr-mogging.git"
  },
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "node scripts/build.js",
    "check": "node --check public/app.js && node --check public/qr-code.js && node --check public/vendor/qrcode.js && node --check scripts/dev.js && node --check scripts/build.js",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.8",
    "jsqr": "^1.4.0"
  },
  "type": "module"
}
